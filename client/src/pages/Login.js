import React, { Fragment } from "react";
import LoginForm from "../components/LoginForm";

const Landing = () => {
    return (
        <Fragment>
            <h1>Login</h1>
            <LoginForm />
        </Fragment>
    );
};

export default Landing;